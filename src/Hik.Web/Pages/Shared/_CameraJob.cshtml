@using Hik.DTO.Config;
@using Hik.DTO.Contracts;
@model TriggerModel

@await Html.PartialAsync("_Trigger.cshtml", Model)

@{
    var config = Model.Dto.ConfigDto as CameraConfig;

    @await Html.PartialAsync("_CameraConfig.cshtml", config)
}