@using Hik.DTO.Config;
@using Hik.DTO.Contracts;
@model TriggerModel

@await Html.PartialAsync("_Trigger.cshtml", Model.Dto)

@{
    var config = Model.Dto.ConfigDto as ArchiveConfig;

    @await Html.PartialAsync("_ArchiveConfig.cshtml", config)
}
